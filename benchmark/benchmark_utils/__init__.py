# Intentionally empty.
#
# Per the benchopt conventions, ``benchmark_utils`` is organised by topic with
# one module per concern (``data``, ``linear_probe``, ``baselines``,
# ``neuralset_task``). Keeping this file empty ensures that importing one topic
# does not pull in the (possibly heavy) dependencies of the others.
