# Shared components for the 4 competition tracks (one module per concern:
# ``data``, ``base_solver``, ``baselines``, ``linear_probe``,
# ``neuralset_task``). Kept empty so importing one topic does not pull in the
# (possibly heavy) dependencies of the others.
